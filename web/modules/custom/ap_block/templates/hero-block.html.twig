{% if background_image is not empty %}
{% set bg_url = file_url(background_image.entity.field_media_image.entity.fileuri) %}
{% endif %}

{{ dump(bg_url) }}

<div class="hero" style="background-image: " > fuck
	<h1>
		{{ hero_block_title }}
	</h1>
	<h3>
		{{ hero_block_subtitle }}
	</h3>
</div>
